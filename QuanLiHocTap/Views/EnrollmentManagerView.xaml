<UserControl x:Class="QuanLiHocTap.Views.EnrollmentManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLiHocTap.Views"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1170">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Border Background="White" CornerRadius="8" Padding="10,5" Width="250" Margin="0,0,10,0" BorderThickness="1" BorderBrush="#DEE2E6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" VerticalAlignment="Center" Margin="5,0"/>
                    <TextBox Grid.Column="1" BorderThickness="0" VerticalContentAlignment="Center" Tag="Tìm học viên..." Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>
            </Border>

            <ComboBox Width="180" Height="40" Margin="0,0,10,0" SelectedIndex="0">
                <ComboBoxItem Content="Tất cả trạng thái"/>
                <ComboBoxItem Content="Đang học"/>
                <ComboBoxItem Content="Chờ xác nhận"/>
                <ComboBoxItem Content="Đã hủy"/>
            </ComboBox>

            <ComboBox Width="180" Height="40" Margin="0,0,10,0" SelectedIndex="0">
                <ComboBoxItem Content="HK1 - 2025-2026"/>
                <ComboBoxItem Content="HK2 - 2025-2026"/>
            </ComboBox>

            <Button Content="Kiểm tra giới hạn (UC03)" Background="#1A73E8" Foreground="White" Padding="15,0" BorderThickness="0" Height="40">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>

        <!-- Main Content Area -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Danh sách đăng ký học phần" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Border HorizontalAlignment="Right" Background="#E6F4EA" CornerRadius="15" Padding="10,5">
                        <TextBlock Text="Tất cả: ??" Foreground="#1E8E3E" FontWeight="Bold" FontSize="12"/>
                    </Border>
                </DockPanel>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding Enrollments}" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          Background="Transparent"
                          BorderThickness="0"
                          RowHeight="60"
                          HorizontalScrollBarVisibility="Auto">
                    
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#6C757D"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,15"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#F1F3F4"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#F8F9FA"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FBFBFB"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <!-- Enrollment ID -->
                        <DataGridTextColumn Header="ENROLLMENTID" Binding="{Binding EnrollmentId}" Width="120" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="10,0"/>
                                    <Setter Property="FontSize" Value="12"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Học viên -->
                        <DataGridTemplateColumn Header="HỌC VIÊN" Width="200">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel VerticalAlignment="Center" Margin="10,0">
                                        <TextBlock Text="{Binding StudentName}" FontWeight="Bold" Foreground="#202124"/>
                                        <TextBlock Text="{Binding StudentId}" Foreground="#70757A" FontSize="11"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Môn học -->
                        <DataGridTextColumn Header="MÔN HỌC / LỚP" Binding="{Binding CourseInfo}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Tín chỉ -->
                        <DataGridTemplateColumn Header="TÍN CHỈ" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#E8F0FE" CornerRadius="12" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0">
                                        <TextBlock Text="{Binding Credits, StringFormat={}{0} TC}" Foreground="#1967D2" FontWeight="Bold" FontSize="11"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Ngày ĐK -->
                        <DataGridTextColumn Header="NGÀY ĐK" Binding="{Binding EnrollDate, StringFormat=dd/MM/yyyy}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Trạng thái -->
                        <DataGridTemplateColumn Header="TRẠNG THÁI" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="6" Padding="10,4" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Đang học">
                                                        <Setter Property="Background" Value="#E6F4EA"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Chờ xác nhận">
                                                        <Setter Property="Background" Value="#FEF7E0"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Đã hủy">
                                                        <Setter Property="Background" Value="#FCE8E6"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Status}" FontWeight="SemiBold" FontSize="12">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Đang học">
                                                            <Setter Property="Foreground" Value="#137333"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Chờ xác nhận">
                                                            <Setter Property="Foreground" Value="#B06000"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Đã hủy">
                                                            <Setter Property="Foreground" Value="#C5221F"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- GPA HK -->
                        <DataGridTextColumn Header="GPA HK" Binding="{Binding Gpa}" Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#1967D2"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Hành động -->
                        <DataGridTemplateColumn Header="HÀNH ĐỘNG" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Hủy ĐK" 
                                            Background="Transparent" 
                                            BorderBrush="#FCE8E6" 
                                            Foreground="#D93025" 
                                            Padding="10,5"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
