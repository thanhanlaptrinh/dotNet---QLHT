<UserControl x:Class="QuanLiHocTap.Views.GradeApprovalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLiHocTap.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" Height="50">
                <Button Command="{Binding ChangeTabCommand}" CommandParameter="Submitted" Background="Transparent" BorderThickness="0" Padding="15,0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <Border x:Name="ActiveLine" Height="3" Background="#1A73E8" VerticalAlignment="Bottom" Visibility="Visible"/>
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Chờ duyệt (Submitted)" FontWeight="Bold" Foreground="#1A73E8" FontSize="14" VerticalAlignment="Center"/>
                        <Border Background="#E8F0FE" CornerRadius="10" Padding="8,2" Margin="8,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="??" Foreground="#1A73E8" FontWeight="Bold" FontSize="11"/>
                        </Border>
                    </StackPanel>
                </Button>

                <Button Command="{Binding ChangeTabCommand}" CommandParameter="Approved" Background="Transparent" BorderThickness="0" Padding="15,0" Margin="10,0,0,0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <Border x:Name="ActiveLine" Height="3" Background="Transparent" VerticalAlignment="Bottom" Visibility="Collapsed"/>
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Đã duyệt (Approved)" FontWeight="SemiBold" Foreground="#9AA0A6" FontSize="14" VerticalAlignment="Center"/>
                        <Border Background="#F1F3F4" CornerRadius="10" Padding="8,2" Margin="8,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="??" Foreground="#5F6368" FontWeight="Bold" FontSize="11"/>
                        </Border>
                    </StackPanel>
                </Button>

                <Button Command="{Binding ChangeTabCommand}" CommandParameter="Published" Background="Transparent" BorderThickness="0" Padding="15,0" Margin="10,0,0,0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <Border x:Name="ActiveLine" Height="3" Background="Transparent" VerticalAlignment="Bottom" Visibility="Collapsed"/>
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Đã công bố (Published)" FontWeight="SemiBold" Foreground="#9AA0A6" FontSize="14" VerticalAlignment="Center"/>
                        <Border Background="#F1F3F4" CornerRadius="10" Padding="8,2" Margin="8,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="?/" Foreground="#5F6368" FontWeight="Bold" FontSize="11"/>
                        </Border>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="#FFF8E1" BorderBrush="#FFE082" BorderThickness="1" CornerRadius="8" Padding="15,12" Margin="0,0,0,20">
            <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="#B78103">
                <Run Text="Các lớp sau đã được GV nộp điểm. Kiểm tra và "/>
                <Run Text="Duyệt (Approve)" FontWeight="Bold"/>
                <Run Text=" để chuyển sang bước tiếp theo."/>
            </TextBlock>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="20" BorderThickness="1" BorderBrush="#E2E8F0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.03"/>
            </Border.Effect>

            <DataGrid ItemsSource="{Binding ApprovalClasses}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#F1F3F4"
                      Background="Transparent"
                      BorderThickness="0"
                      RowHeight="65">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#94A3B8"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="LỚP HỌC PHẦN" Width="250">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center" Margin="10,0">
                                    <TextBlock Text="{Binding ClassId}" FontWeight="Bold" Foreground="#1E293B" FontSize="13"/>
                                    <TextBlock Text="{Binding CourseName}" Foreground="#64748B" FontSize="11" Margin="0,2,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="GV PHỤ TRÁCH" Binding="{Binding Lecturer}" Width="180">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="10,0"/>
                                <Setter Property="Foreground" Value="#334155"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="SĨ SỐ" Binding="{Binding StudentCount}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                <Setter Property="Margin" Value="10,0"/>
                                <Setter Property="Foreground" Value="#334155"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="ĐÃ NHẬP" Width="130">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12" Padding="12,4" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFullyEntered}" Value="True">
                                                    <Setter Property="Background" Value="#E6F4EA"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsFullyEntered}" Value="False">
                                                    <Setter Property="Background" Value="#FEF7E0"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding EnteredCount}" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsFullyEntered}" Value="True">
                                                            <Setter Property="Foreground" Value="#137333"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsFullyEntered}" Value="False">
                                                            <Setter Property="Foreground" Value="#B06000"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="/" FontWeight="Bold" Margin="2,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsFullyEntered}" Value="True">
                                                            <Setter Property="Foreground" Value="#137333"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsFullyEntered}" Value="False">
                                                            <Setter Property="Foreground" Value="#B06000"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="{Binding StudentCount}" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsFullyEntered}" Value="True">
                                                            <Setter Property="Foreground" Value="#137333"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsFullyEntered}" Value="False">
                                                            <Setter Property="Foreground" Value="#B06000"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="ĐTB LỚP" Binding="{Binding ClassAverage}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#1A73E8"/>
                                <Setter Property="Margin" Value="10,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="NỘP LÚC" Binding="{Binding SubmittedTime}" Width="180">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#64748B"/>
                                <Setter Property="Margin" Value="10,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="HÀNH ĐỘNG" Width="220">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="Xem chi tiết" 
                                            Background="White" 
                                            Foreground="#475569" 
                                            BorderBrush="#E2E8F0" 
                                            BorderThickness="1" 
                                            Padding="10,5" 
                                            Margin="0,0,8,0" 
                                            FontWeight="SemiBold"
                                            Height="32">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="6"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>

                                    <Button Content="UC06: Duyệt" 
                                            Background="#D97706" 
                                            Foreground="White" 
                                            BorderThickness="0" 
                                            Padding="12,5" 
                                            FontWeight="Bold"
                                            Height="32">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="6"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
